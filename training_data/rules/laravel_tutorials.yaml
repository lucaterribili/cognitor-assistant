# Rules per search_tutorial (integrazione Laravel/Programmato Backoffice)

rules:
  search_tutorial:
    slots:
      query:
        required: true
        type: string
        entity: TUTORIAL_QUERY

    default: __search_tutorial
    wait: search_tutorial_wait_response
    fallback: search_tutorial_unsupported_response
