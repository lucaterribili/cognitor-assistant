# Culture rules - Intent per cultura, arte, storia

rules:
  ask_culture:
    slots:
      query:
        required: true
        type: string
        entity: KEYWORD
    default: __web_search
    wait: ask_culture_wait_response

  ask_definition:
    default: ask_definition_response

