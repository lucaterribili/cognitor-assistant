# Food rules - Intent per ricette e cucina

rules:
  ask_recipe:
    slots:
      query:
        required: true
        type: string
        entity: DISH
    default: __web_search
    wait: ask_recipe_wait_response
